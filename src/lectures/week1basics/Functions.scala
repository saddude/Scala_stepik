package lectures.week1basics

object Functions extends App{
  def aCondition(): Boolean = if ( 1 > 2) true else false

  def someFunc(x: Int, y: Int): Unit = {
    if (aCondition) x * 3 else  false(y)
  }
}
